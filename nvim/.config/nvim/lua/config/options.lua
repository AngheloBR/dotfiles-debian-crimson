-- Opciones propias (LazyVim ya trae buenos defaults: numeros relativos,
-- clipboard del sistema, etc.). Solo lo que cambia respecto a eso.

-- corrector ortografico en espanol para texto/markdown (z= sugiere)
vim.opt.spelllang = { "es", "en" }

-- limite visual a 80 columnas en el color del borde inactivo
vim.opt.colorcolumn = "80"
